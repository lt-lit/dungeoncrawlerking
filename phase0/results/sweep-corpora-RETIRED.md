# The `sweep-*.jsonl` corpora are crumble-era output — history, not evidence

Retired 2026-09-01 with the harness that produced them
(`harness/sweep.mjs` + the crumble-system game loop, now frozen in
`harness/legacy/` — see its README). Everything in `sweep-smoke`,
`sweep-tiny`, `sweep-starter`, `sweep-starter-native` and
`sweep-starter-bk` (JSONL + summaries) was played under the RETIRED
crumble system (repetition + fixed-cadence pacing) on the retired
generated-arena bed — two generations before the shipped v3 Director and
the designer-locked stage bed.

What still stands from them: the config-history lessons in
`sweep-starter-findings.md` (the A-prime no-draw config, the bare-army
in-grammar validation, the §7 sweep-validity law itself). What does NOT
stand: any number about pacing, game length under regeneration, crumble
rates, or the alarm metric. Phase 1.5 Director data comes from
`harness/godlab/` (canon DuelController + v3 Director on the stage bed);
its corpora land in `results/godlab/`.
